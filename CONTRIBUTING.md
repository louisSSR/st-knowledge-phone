# 初版维护约束

1. 核心零后端、零必需第三方酒馆插件，默认不调用外部 API。
2. 每个 Provider 必须接收完整 SearchContext；搜索、联想、阅读与收藏都遵守硬过滤。
3. 知识包与核心源码分离，数据不得携带可执行脚本。纯文本渲染，不将资料插入聊天。
4. 全文扫描只在 Worker。限制结果数量，正文按点击加载，关闭终止 Worker。
5. UI 使用 Shadow DOM，不污染宿主 CSS；宿主 API 只允许在 platform/sillytavern.ts 访问。
6. Theme 与 Provider 通过注册表扩展，不给主流程塞跨功能条件分支。
7. 不修改宿主原函数、fetch、原型、聊天渲染器；不定时扫描 DOM。
8. schemaVersion 升级使用显式迁移；不猜测兼容旧包，不覆盖同 ID 包。
9. 监听器、Worker、DB 句柄必须有清理，异步结果必须检查当前聊天和请求代号。
10. 先保持小文件、简单依赖和可读函数，再加新功能。新增功能不得破坏时间线、隐私和安装便利。
11. 源码检查、浏览器预览、真实酒馆、人工验收、安装、Git 与发布是不同的证据或授权门。
